# 需求与目标

## 原始需求

基于 Relume 导出的网页原型，结合既有个人作品集内容，重新搭建一个视觉更高级、适合投递简历的个人作品集网页。新项目必须独立放在 `Projects/project002_个人作品集Next版/`，不改动旧项目 `project001_个人作品集网页/`。

## 目标

1. 使用 Next.js App Router 搭建 `/` 与 `/portfolio`。
2. 视觉以深色高级影像官网为主，参考 Relume 原型，不做廉价开屏动画。
3. 保持 JSON 数据驱动，方便后续替换 COS URL 清单和个人资料。
4. 支持桌面端和手机端浏览。
5. 图片、视频和 PDF 都在站内预览，不触发下载。

## 交付物

1. Next.js 项目代码与组件。
2. `public/data/raw-assets.json`、`portfolio-items.json`、`pdf-pages.json`、`profile.json`。
3. PDF 页面图片、视频 poster、微信二维码和 Relume hero 图。
4. Playwright 视觉检查脚本与截图。

## 验收标准

1. `pnpm test` 通过。
2. `pnpm build` 通过。
3. `pnpm visual:check` 通过。
4. 首页精选 6 项均可打开详情。
5. 作品集分类筛选可用。
6. 图片弹窗可左右切换，PDF 可按页阅读，视频可播放。
7. 手机端无横向溢出。

## 限制条件

1. 不修改旧项目代码。
2. 不执行 Git commit、push、reset 等版本操作。
3. 不做部署上线。
4. 不删除项目生成的素材和预览文件。

## 非目标

1. 不制作简历下载页面。
2. 不接入 CMS 或后台管理。
3. 不追求复杂 3D 或开屏特效。

## 待确认问题

1. 上线部署、域名和备案策略后续单独确认。
2. 最终首页视觉是否继续基于当前 Relume 风格微调，后续由用户预览后决定。

## 数据源记录

- 当前 COS URL 清单：`04_素材与参考/cos-object-list-1783026698139.csv`
- 上一版清单保留归档：`04_素材与参考/cos-object-list-1783004618848.csv`
