# 过程记录

## 2026-07-03

### 已完成

- 创建独立新项目 `Projects/project002_个人作品集Next版/`。
- 归档新 COS CSV 与 Relume ZIP 到 `04_素材与参考/`。
- 归档并切换最新 COS URL 清单 `cos-object-list-1783026698139.csv`。
- 清洗 COS URL，生成作品数据、PDF 页面 manifest、视频 poster。
- 搭建 Next.js App Router 首页 `/` 和作品集页 `/portfolio`。
- 实现分类筛选、详情抽屉、图片弹窗左右切换、PDF 站内阅读、视频播放。
- 使用 `impeccable` 做自审，补充 `PRODUCT.md` 和 `DESIGN.md`。
- 新增 `scripts/visual-check.mjs` 进行桌面端、移动端和关键交互截图检查。

### 关键决策

- 旧项目只作为参考源，不直接修改。
- 首页视觉采用 Relume 的深色影像官网方向，不恢复开屏动画。
- 导航保留品牌、作品集、联系我，不加入关于我或简历下载。
- PDF 转图片后在抽屉内纵向阅读，避免浏览器下载 PDF。
- 作品详情统一在当前页抽屉打开，保持底层页面上下文。

### 变更记录

- 修复 Next.js `@/` 路径别名缺失导致的构建失败。
- 修复抽屉遮罩 blur 导致移动端详情截图偏糊的问题。
- 收敛标题最大字号和圆角尺度，增加键盘焦点样式。
- 根据复审反馈，将首页精选作品改为规整 3×2 栅格。
- 将“活动”和“蔚来 NIO House”改为总项目卡片内的子项目瀑布流，再进入具体图集。
- 联系方式每一行增加右侧“复制”操作。
- 使用最新 URL 清单重新生成数据，并将“活动”封面固定为用户指定的 `Image936.webp`。
- 作品集页卡片移除纵向错位偏移，改为稳定的左到右对齐网格。
- 商业摄影“活动”“蔚来 NIO House”等多层级项目保留子项目卡片，但具体图集改为液态模糊弹窗打开，避免图集被放在长列表下方。
- 修复子项目图集弹窗在大量图片下的网格行高压缩问题，改为固定比例换行缩略图，点击后进入完整比例灯箱。
- 更新视觉检查脚本，覆盖子项目弹窗、弹窗内图片点击、图片灯箱路径。
- 根据页面批注更新首页和作品集页文案，将微电影展示标题统一为“微电影-乘翼归来”，里程碑标题改为“微电影-乘翼归来-广东省一等奖”。
- 将“寻味——顺德牛乳”展示标题调整为“数字短片：寻味——顺德牛乳”。
- 将商业摄影展示顺序固定为：活动、人像、蔚来 NIO House、风光、产品、人文。
- 将首页里程碑奖项代表图替换为用户提供的电影剧照，并将“微电影-乘翼归来”视频卡片封面恢复为默认视频封面。
- 归档 `cos-object-list-1783430664310_平面视觉补充.csv`，作为平面视觉补充清单合并进完整 COS 数据。
- 数据生成脚本支持完整清单 + 补充清单合并，并按清洗后的公开 URL 去重，新增 `科技梦想`、`小鹿激光`、`一叶子`、`Xiao cang` 四个平面视觉图片作品。

### 风险与阻塞

- 当前仅做本地预览，尚未评估部署平台、中国大陆访问速度、域名和备案。
- 远程 COS 图片数量较多，线上性能还需在部署前继续压缩和懒加载策略评估。

### 下一步

- 用户预览当前 Next 版视觉与交互。
- 根据反馈继续做视觉 polish 或进入部署方案评估。

## 2026-07-07

### 已完成

- 在正式改版前创建可回退备份：`04_素材与参考/backups/20260707_审查前正式版/`。
- 将首页精选作品改为主推墙结构：`活动` 为超大主视觉，`蘑菇旅行` 和 `IP 鼠小仓` 为中型项目，其余精选归入小型项目。
- 为作品集页卡片增加内容类型视觉语言：商业摄影偏横向大封面，影视后期增加胶片/播放感，PDF 与平面视觉增加阅读物封面感。
- 将卡片打开详情抽屉改为 View Transition shared-element 过渡，封面图在卡片和抽屉头图之间形成连续关系。
- 收敛毛玻璃使用范围，重点用于筛选条、详情抽屉、子图集弹窗和交互浮层。
- 新增 `/design-preview` 作为视觉方向对照预览页，保留用于后续比较。

### 关键决策

- 正式页面不采用夸张 3D 或开屏动画，动效集中在作品卡片、筛选切换和详情抽屉的空间连续性。
- 桌面端保留 PDF/平面视觉的纸张层次感，手机端收敛为干净封面，避免两列卡片显得拥挤。
- 保留原有 JSON 数据驱动和抽屉/PDF/视频/图集能力，只升级展示层和交互层。

### 验证

- `pnpm test` 通过：2 个测试文件，8 个测试通过。
- `pnpm build` 通过，路由包含 `/`、`/portfolio`、`/design-preview`。
- 已进行桌面端首页精选墙、作品集抽屉，以及手机端精选墙的浏览器快检。

## 2026-07-08

### 已完成

- 将首页精选作品从主推墙改为轮播结构：主视觉卡片、精选队列、左右切换按钮和页码点。
- 修正平面视觉作品标题：`小鹿激光` 改为 `小鹿极光`，同步更新生成脚本、测试和当前 JSON 数据。
- 为平面视觉中的单张 image 海报增加详情抽屉内的完整比例海报预览，点击可进入大图灯箱。
- 单张海报不再重复显示“图集预览”缩略图区，避免信息重复。
- 删除影视后期卡片和设计预览中的胶片条纹效果，仅保留视频封面和播放标识。

### 验证

- `pnpm test` 通过：2 个测试文件，8 个测试通过。
- `pnpm build` 通过，路由包含 `/`、`/portfolio`、`/design-preview`。
- 已进行首页轮播、作品集首屏、`小鹿极光` 海报详情的浏览器快检。

### 追加调整

- 将首页精选作品轮播升级为自动轮播，默认约 4.8 秒切换一次。
- 自动轮播支持鼠标悬停、键盘聚焦、详情抽屉打开时暂停，保留左右按钮和圆点手动切换。
- 增加轻量进度条，用于提示当前作品自动轮播进度。
- 浏览器验证页码从 `01 / 06` 自动切换到 `02 / 06`。
- 根据 Semi Design Carousel 参考，将首页精选作品预览改为单张大主视觉 Carousel：移除队列缩略卡，仅保留大图、浮层标题、箭头、圆点和进度条。
- 调整窄视口下的标题与控件位置，避免箭头/圆点压住作品标题。
- 根据移动端注释，将轮播左右切换从底部组合按钮改为 banner 两侧悬浮箭头，并移除底部自动播放进度条，仅保留页码与圆点状态。
- 根据后续移动端注释，删除轮播页码胶囊显示，并将精选作品切换从内容替换改为横向 track 平移动效；手机端验证点击右箭头后 track 发生位移。
- 修正精选轮播标题在窄屏预览中过大的问题：标题改为按轮播容器宽度自适应，并将旧版精选墙的 `nth-child` 标题规则限定到 `.featured-grid`，避免误伤 carousel。
- 根据移动端注释，将首页精选区的「全部作品」入口从标题行移到轮播图下方；移动端按钮与轮播等宽，桌面端靠右显示。
- 为首页、作品集页、普通作品卡片、精选轮播、里程碑、联系区、详情抽屉与子图集标题补充容器宽度自适应和防溢出规则；已在 474px、390px 视口滚动扫描首页、作品集页和详情抽屉，未发现标题横向溢出。
- 针对首页 Hero 姓名显示补充不可换行规则：将中文名与英文名分别包裹为 `hero-name-zh`、`hero-name-en`，并让整组姓名在移动端按视口收缩且保持单行；已验证 474px、390px、360px、320px、280px 视口均无换行和横向溢出。
- 执行 B 级动效改造：为作品详情抽屉、商业摄影子项目弹窗和图片灯箱增加关闭中的延迟卸载状态，避免点击关闭后直接硬切消失。
- 为图片灯箱增加打开、关闭和左右切换方向动效；左右切换使用轻微位移、模糊和淡入，保持克制，不引入新动画库。
- 桌面端抽屉关闭向右收回，手机端底部抽屉关闭向下收回；所有新增动效继续遵守 `prefers-reduced-motion`。
- 验证：`pnpm test` 通过 2 个测试文件、8 个测试；`pnpm build` 通过；使用系统 Chrome 完成桌面和 390px 移动端交互快检，抽屉、子项目弹窗、灯箱关闭状态和灯箱左右切换均可用。
- 执行最终审查后的全量修复：补充抽屉、子图集、灯箱的焦点陷阱和焦点恢复，避免键盘焦点跑到底层作品卡片。
- 修复商业摄影中文 ID 被清洗后导致 `view-transition-name` 重复的问题：transition id 增加稳定 hash 后缀，消除浏览器 shared-element 过渡中止警告。
- 补充 `app/icon.svg`，解决 favicon 缺失；导航增加 skip link，移动端品牌名改为 `Jhin H`，避免窄屏截断。
- 首页轮播圆点改为小视觉点 + 大触控热区，满足移动端触控尺寸；作品卡片和详情图片补充 `decoding`、`fetchPriority`、`sizes` 等加载提示。
- 最终验证：`pnpm test` 通过，`pnpm build` 通过；使用系统 Chrome 覆盖 1440、820、474、390、320px 的首页和作品集页，结果为无控制台错误、无失败资源、无横向溢出、无破图、无缺失 alt、无小触控目标、无标题溢出；弹窗焦点路径和 reduced motion 关闭路径均通过。
- 使用 `icon-system` 重新审查图标系统，将主站图标收敛到 `components/Icon.jsx` 统一尺寸入口；导航、按钮、作品卡片、联系区、抽屉、子图集和灯箱均改用 `AppIcon`，保留 Lucide 图标库和现有视觉语义。
- 新增 `lib/cosImage.js`，统一生成 COS 下载时图片处理 URL；项目封面与商业摄影子项目封面在数据生成时写入 `coverSet`，图集缩略图在前端运行时派生，灯箱仍使用原图。
- 新增交付文档 `03_交付成果/cos-thumbnail-strategy.md`，记录 COS 缩略图、多尺寸封面清单、尺寸规则和后续部署建议。
- 修复 `scripts/visual-check.mjs`：优先使用系统 Chrome，避免本地缺少 Playwright 自带 Chromium 时无法审查；PDF 检查目标改为稳定点击 `蘑菇旅行`，不再受新增海报排序影响。
- 运行完整 `pnpm generate:data`，恢复视频 poster 和 PDF 首页封面，消除因仅生成 raw data 导致的空 `src` 警告。
- 验证：`pnpm test` 通过，`pnpm build` 通过，`pnpm visual:check` 通过；额外使用系统 Chrome 检查桌面、390px、320px 的 `/` 与 `/portfolio`，结果为无控制台错误、无破图、无缺失 alt、无横向溢出，并确认 COS 图片实际渲染为 `imageMogr2` 多尺寸 `srcset`。
- 执行最终只读审查与清理评估：重新通过 `pnpm test`、`pnpm build`、`pnpm visual:check`；扫描 `project002` 与 `project001` 体积、缓存、未引用 public 资源、历史项目价值。
- 新增交付报告 `03_交付成果/final-audit-cleanup-report.md`，记录最终网页健康度、未引用素材、可清理缓存、`project001` 保留/删除判断和三种清理策略。
- 根据用户确认执行方案 A 缓存清理：删除 `project002/tmp/videos/`、`project002/.next/`、`project002/tmp/test-*.json`、`project002/03_交付成果/previews/debug-*.png`、`project001/tmp/videos/`、`project001/node_modules/`。
- 清理结果：`project002` 从约 `3.1G` 降至约 `611M`，`project001` 从约 `2.0G` 降至约 `418M`；4174 本地预览进程已停止；`pnpm test` 通过，未运行 `pnpm build`，避免重新生成 `.next` 构建缓存。

### 追加发布前优化

- 根据用户确认，移除正式站点中的 `/design-preview` 路由和 `DesignPreview` 组件，并清理对应全局 CSS；正式构建路由仅保留 `/`、`/portfolio`、`/icon.svg` 和 404。
- 新增 `scripts/imageMetadata.mjs` 与 `scripts/enrichImageMetadata.mjs`，为本地封面、视频 poster、PDF 页面图和微信二维码补充宽高元数据；当前 114 张 PDF 页面图已全部写入尺寸。
- 新增 `scripts/generatePdfCosManifest.mjs`、`pnpm generate:pdf-cos-manifest` 和 `03_交付成果/cos-pdf-page-deployment.md`，为后续将 PDF 页面图迁移到 COS/CDN 提供上传清单和执行策略。
- 为商业摄影大图集增加分段加载：图集弹窗和普通图集预览默认先显示 24 张，再通过“加载更多图片”继续展开，灯箱仍可浏览完整图集。
- 为 `app/layout.jsx` 补充移动端 `themeColor` 与暗色 `colorScheme`。
- 根据用户授权，删除历史旧项目 `Projects/project001_个人作品集网页/`。
- 为第 4 项“高级感继续提升”生成独立预览稿：`03_交付成果/previews/premium-direction-preview.html` 与 `premium-direction-preview.png`，包含 A 影像展陈、B contact sheet、C shared drawer 三个方向。

### 验证

- `pnpm test` 通过：2 个测试文件、8 个测试。
- `pnpm build` 通过：正式构建不再包含 `/design-preview`。
- `pnpm visual:check` 通过：首页、作品集、抽屉、PDF、视频、商业摄影子项目、图片灯箱和移动端截图均生成成功。
- 数据检查通过：10 个 COS 封面写入响应式 `coverSet`，18 个本地封面写入尺寸，114/114 张 PDF 页面图写入尺寸，微信二维码尺寸为 `697x697`。

## 2026-07-09

### 已完成

- 根据用户要求，在接入 C 方案详情前创建当前网页备份：`04_素材与参考/backups/20260709_C方案接入前网页备份/`。
- 新增 `public/data/project-details.json`，以 JSON 数据驱动方式保存 8 个重点项目详情，不把简历或项目文案硬编码进组件。
- 为作品详情抽屉接入 C 方案信息层：项目定位、我的角色、交付内容、使用工具、项目亮点和浏览提示。
- 首页精选、里程碑模块和作品集页共用同一份项目详情数据，避免后续维护多处文案。
- 补充 C 方案详情样式：桌面端三栏元信息 + 亮点/浏览提示双栏；移动端自动折叠为单列。
- 修正 Vitest 扫描范围，避免项目备份目录中的测试文件被重复执行。

### 验证

- `pnpm test` 通过：2 个测试文件、8 个测试。
- `pnpm build` 通过：正式路由仍为 `/`、`/portfolio`、`/icon.svg`、404。
- `pnpm visual:check` 通过，覆盖首页、作品集、抽屉、PDF、视频、商业摄影子项目、灯箱和移动端截图。
- 本地完整预览已启动：`http://127.0.0.1:4174`。

### 追加修复

- 修复打开作品详情时底层项目卡片短暂跳动、压到底部后消失的问题。
- 根因：详情抽屉打开时使用 `document.startViewTransition` 截取了整个页面 root 快照，导致底层作品网格也参与全局过渡。
- 处理：作品详情打开改为只使用抽屉自身进入动效，不再触发全页面 View Transition；分类筛选仍保留 View Transition。
- 补充 `scrollbar-gutter: stable`，避免打开抽屉时页面滚动条空间变化造成宽度微跳。
- 验证：打开作品详情前、80ms、500ms 的底层卡片页面坐标一致；`pnpm test`、`pnpm build`、`pnpm visual:check` 均通过。
- 针对桌面端/移动端差异补充响应式回归审计：`scripts/visual-check.mjs` 新增 1440px、390px、721px 窄屏路径断言，覆盖详情抽屉贴顶、子图集全高贴顶、顶部栏不透明遮底、商业摄影卡片/图集标题不显示张数、二维码方形比例、打开抽屉时底层卡片不位移、横向溢出等问题。
- 修复移动端商业摄影子图集弹窗仍像半截浮层悬空的问题：移动端子图集改为全屏贴顶，头部改为 sticky 顶栏并补充明确不透明背景，避免滚动时露出底层页面。
- 验证：`pnpm test` 通过 2 个测试文件、8 个测试；`pnpm build` 通过；`pnpm visual:check` 通过，额外生成 `portfolio-subgallery-narrow.png` 用于 721px 窄屏子图集回归检查。
- 根据 taste / impeccable 审美审查结论，在不修改正式网页的前提下创建当前版本备份：`04_素材与参考/backups/20260709_高级视觉预览前备份/`，排除 `.next`、`node_modules`、大缓存和备份目录本身。
- 新增高级视觉优化预览页：`03_交付成果/previews/premium-polish-visual-preview.html`，覆盖四个方向：2 首页节奏、3 去模板化标题、4 字体气质、5 作品观看动效。
- 导出对应预览图：`premium-polish-01-rhythm.png`、`premium-polish-02-language.png`、`premium-polish-03-type.png`、`premium-polish-04-motion.png`；预览仅用于决策，不接入正式路由。
- 根据用户反馈调整第 5 项动效策略：不采用“详情下置于作品流之后”的结构，改为“卡片封面延展到当前页右侧抽屉 / 手机全屏详情”，避免作品数量多时 HR 需要继续下滑寻找详情。
- 新增完整网页预览：`03_交付成果/previews/premium-polish-full-page-preview.html`，将 2 首页节奏、3 去模板化标题、4 字体气质、修改后的 5 卡片到抽屉动效合并为一版完整首页 + 作品区 + 联系区预览。
- 导出完整预览截图：`premium-polish-full-desktop.png`、`premium-polish-full-desktop-drawer.png`、`premium-polish-full-mobile.png`、`premium-polish-full-mobile-drawer.png`；浏览器验证无控制台错误、无横向溢出，桌面和手机端抽屉均可打开。
- 根据用户对完整预览的标注，删除额外的 `PORTFOLIO PATH` 作品集展示段，避免首页出现重复的作品列表入口。
- 将完整预览的精选区文案从抽象策展表达改回直接标题「精选作品」，并把导航「作品集」锚点改回 `#featured`。
- 保留用户认可的 Hero、能力区和代表项目区；重新导出桌面/移动端完整预览与抽屉截图，验证无控制台错误、无横向溢出，抽屉仍在当前页面打开。
- 根据用户后续标注，将完整预览的精选区恢复为旧版轮播结构，并恢复下方「全部作品」按钮；同时重新加入 `#portfolio` 全部作品预览段。
- 修复完整预览联系区字段名缺失问题：电话、邮箱、微信号均恢复清晰标签。
- 重新导出完整预览桌面/移动端截图，验证轮播切换、全部作品锚点、抽屉打开、联系标签、无横向溢出和无控制台错误均通过。
- 根据用户纠正，移除完整预览中错误恢复的首页内联 `#portfolio` 作品区；「作品集」导航和「全部作品」按钮改为跳转独立作品集页 `http://127.0.0.1:4174/portfolio`，对应优化前已有完整详情抽屉的作品集页面。
- 重新验证完整预览：`PORTFOLIO PATH` 文案和 `section#portfolio` 均不存在；轮播、抽屉、联系标签、桌面/移动端无横向溢出均通过。
- 根据用户确认，将高级视觉预览中认可的 Hero 头部区和获奖区接入正式首页：Hero 改为 `Visual Content Creator` 金色胶囊、宋体中文大标题与暗色山路影像；获奖区改为紧凑横向代表项目条，移除原先嵌套的单作品卡片。
- 获奖区按钮改为 `查看项目详情` 并跳转 `/portfolio`，避免在代表项目区再次出现巨大作品卡导致阅读节奏断裂。
- 验证：`pnpm test` 通过，`pnpm build` 通过，`pnpm visual:check` 通过；额外导出 `home-premium-hero-milestone-desktop.png` 与 `home-premium-hero-milestone-mobile.png`，确认无控制台错误、无横向溢出，Hero 字体与 Milestone 结构符合目标。
- 根据用户标注，删除所有作品详情抽屉中的「浏览提示 / WORK PREVIEW」信息块，仅保留角色、交付内容、工具与项目亮点；项目亮点块改为横跨详情内容区，避免删除右侧块后出现空列。
- 验证：文本扫描已无「浏览提示」「WORK PREVIEW」「detail-quick-info」；`pnpm test`、`pnpm build`、`pnpm visual:check` 均通过。

### 路由切换动效优化

- 根据用户确认，优化首页与作品集之间的切换动效：导航「作品集」、Logo 返回首页、Hero「查看作品」、精选区「全部作品」和获奖区「查看项目详情」均接入同一套页面切换动画。
- 启用 Next.js 16 原生 `experimental.viewTransition`，使用 `Link transitionTypes` 和页面级 `ViewTransition`，替代手动 `document.startViewTransition` 方案，避免路由 DOM 更新超时导致的 transition abort。
- 固定导航栏设置为 `persistent-nav`，在 View Transition 中保持稳定；页面主体采用克制的模糊失焦、轻微位移和清晰入场效果，前进/返回方向有细微差异但不做重滑动。
- 保留「联系我」为普通锚点滚动，不触发路由级过渡。
- 验证：`pnpm test` 通过，`pnpm build` 通过，`pnpm visual:check` 通过；自动化浏览器验证首页到作品集、Logo 回首页、Hero 按钮和移动端导航均触发 View Transition，且无控制台错误、无横向溢出。

### 获奖区项目详情深链修复

- 修复首页获奖区「查看项目详情」只跳转作品集页、没有打开微电影详情的问题。
- 按钮改为 `/portfolio#work=项目ID` 深链；作品集页在客户端读取 hash，并自动打开对应作品详情抽屉，保持 `/portfolio` 仍为静态路由。
- 为根 `<html>` 补充 `data-scroll-behavior="smooth"`，消除 Next.js 在路由切换时对全局 smooth scroll 的开发警告。
- 验证：`pnpm test` 通过，`pnpm build` 通过且 `/portfolio` 仍为静态页，`pnpm visual:check` 通过；内置浏览器验证从首页获奖区点击后进入 `portfolio#work=...`，自动打开「微电影-乘翼归来」详情，类别为「影视后期」，视频预览存在，无横向溢出。

### 最终封装与部署规划

- 根据用户确认“网页已经没有问题”，整理最终交付说明：`03_交付成果/project-handoff-deployment.md`，记录本地运行、发布前检查、打包策略、GitHub 备份建议和国内访问部署路线。
- 生成干净源码交付包：`03_交付成果/jhin-h-portfolio-next-source-20260709.zip`。
- 生成配套 SHA256 校验文件：`03_交付成果/jhin-h-portfolio-next-source-20260709.sha256`。
- 打包时保留可运行源码、`public/` 资源、数据、脚本和项目文档；排除 `node_modules/`、`.next/`、`tmp/`、历史备份、Relume 参考源、预览页、已有 zip 包和编辑器临时文件。
- 验证压缩包内容未包含上述排除目录，最终包大小约 `146M`，项目目录整体约 `1.3G`。
- 部署建议明确为：GitHub private repo 用于版本备份和回滚；国内访问最终优先腾讯云 COS + CDN + 自定义域名 + HTTPS，若使用大陆节点需提前准备 ICP 备案。
- 未执行 GitHub 上传、Git commit/push 或线上部署，避免在用户未明确授权前改变远端状态。

### GitHub 备份准备

- 根据用户要求“先进行 GitHub 的备份工作”，初始化本地 Git 仓库，主分支为 `main`。
- 新增 `.gitignore`，排除 `node_modules/`、`.next/`、`tmp/`、交付 zip、SHA256 校验文件、预览截图、历史备份、Relume 解压参考源和编辑器临时文件。
- 创建首个本地备份提交：`20d0960 Initial portfolio backup`，提交内容包含 Next.js 源码、`public/` 站内资源、数据 JSON、脚本、项目文档和必要 CSV 清单。
- 当前机器未检测到可用 GitHub CLI，也未发现 GitHub token；远端 GitHub private repo 仍需用户提供仓库地址后再执行 `git remote add origin ...` 与 `git push -u origin main`。
- 通过用户已登录的 Chrome 创建 GitHub private repo：`https://github.com/jhinh915457389-sys/jhin-h-portfolio-next`。
- 远端已绑定为 `origin`；由于本机无 GitHub CLI、SSH key 或 credential helper，改用用户授权创建的 30 天 fine-grained token 做一次性推送，token 仅限当前仓库且仅授予 `Contents: Read and write`。

### GitHub Pages 静态部署配置

- 根据用户要求继续推进“上传源文件并生成 GitHub 网页域名”，为 Next.js 项目补充 GitHub Pages 部署配置。
- 新增 `.github/workflows/pages.yml`：推送到 `main` 后自动安装依赖、执行静态构建、上传 `out/` 并部署到 GitHub Pages。
- 调整 `next.config.mjs`：保留本地开发根路径，同时在 `GITHUB_PAGES=true` 时启用 `basePath` 与 `assetPrefix`，目标路径为 `/jhin-h-portfolio-next`。
- 新增 `lib/sitePath.js`，统一处理本地 `/assets/...` 资源路径；GitHub Pages 构建时通过 `NEXT_PUBLIC_BASE_PATH=/jhin-h-portfolio-next` 自动加前缀，避免图片、PDF 页图、视频封面、二维码和字体在项目域名下 404。
- 将字体 `@font-face` 从全局 CSS 移到 `app/layout.jsx`，由同一套路径工具生成，避免 CSS 内绝对 `/assets/fonts/...` 在 GitHub Pages 子路径下失效。
- 验证：`pnpm test` 通过 2 个测试文件、8 个测试；`pnpm build` 通过；`GITHUB_PAGES=true NEXT_PUBLIC_BASE_PATH=/jhin-h-portfolio-next pnpm build` 通过。
- 静态导出检查：`out/index.html`、`out/portfolio/index.html` 和 `out/_next/static` 中已无裸 `src="/assets"`、`href="/assets"`、`url(/assets)` 或裸 `/_next` 引用；实际页面资源均指向 `/jhin-h-portfolio-next/...`。
- 注意：`out/data/*.json` 会因 `public/data` 原样复制保留原始 `/assets/...`，但当前页面不在浏览器端 fetch 这些 JSON，实际渲染数据已在构建期完成路径前缀处理。

### 移动端标题与复制交互修复

- 根据用户手机端审查反馈，调整首页获奖区标题结构：将「微电影-乘翼归来-广东省一等奖」拆成两行，其中「微电影-乘翼归来」作为独立 `nowrap` 行，避免窄屏下“乘翼归来”四字被拆开换行。
- 为联系区复制按钮补充移动端兼容兜底：优先使用 `navigator.clipboard.writeText`，在不支持或非安全上下文时回退到隐藏 `textarea + execCommand('copy')`，失败时弹出手动复制提示，不再误显示已复制。
- 修复本地 `4174` 预览进程无响应问题：强制结束卡住的旧 `next-server` 进程，并重新启动 `pnpm dev --hostname 127.0.0.1 --port 4174`，本地审查地址保持不变。
- 验证：`pnpm test` 通过 2 个测试文件、8 个测试；`pnpm build` 通过；使用本机 Chrome 模拟 390px 手机宽度，确认「微电影-乘翼归来」宽度等于标题容器宽度且保持单行，电话复制按钮点击后剪贴板内容为 `18279056626`。

### GitHub 推送与 Cloudflare 自动构建同步

- 根据用户确认，准备将移动端标题和联系区复制修复推送到 GitHub `main`，由 Cloudflare 连接的 Git 仓库自动触发构建。
- 推送时发现远端已有 `Enable static export for Cloudflare deployment` 提交，本地先 rebase 到远端之上，确认 `next.config.mjs` 与远端静态导出配置一致。
- GitHub 拒绝包含 `.github/workflows/pages.yml` 的推送，因为当前 token 未授予 `workflow` 权限；考虑到 Cloudflare 自动构建不依赖 GitHub Actions，移除该 workflow 文件，避免扩大 token 权限。

### 移动端手势增强

- 根据用户希望手机端支持常用手势的反馈，为首页精选作品轮播接入横向滑动：左滑进入下一件精选作品，右滑回到上一件精选作品，同时保留原有自动轮播、左右箭头、圆点与点击卡片打开详情。
- 为图片灯箱接入横向滑动：打开图集或海报大图后，左滑下一张、右滑上一张，与原有键盘方向键和左右按钮保持一致。
- 新增复用型 `useSwipeGesture` 逻辑，设置横向阈值和纵向容错，避免普通上下滚动误触；滑动完成后短暂抑制点击，避免轮播滑动后误打开卡片。
- CSS 为轮播主区域和灯箱补充 `touch-action: pan-y pinch-zoom` 与 `user-select: none`，让竖向滚动和横向切换边界更清晰。
- 验证：`pnpm test` 通过 2 个测试文件、8 个测试；`pnpm build` 通过；使用本机 Chrome 390px 手机视口模拟触摸，首页轮播从「活动」左滑到「蘑菇旅行」，作品集灯箱从 `1 / 117` 左滑到 `2 / 117`，无 console error/page error。
- 针对微信/手机浏览器中 PDF 页面被拉伸的问题，补充 PDF 阅读器比例约束：PDF 页图片显式 `height: auto`、`object-fit: contain`，并用原始 `width / height` 写入 `aspect-ratio`，避免内置浏览器按容器高度强制变形。
- PDF 页面改为可点击预览：点击任意 PDF 页进入灯箱，手机端可左右滑翻页，和普通图集保持一致。
- 为手势逻辑增加 touch 事件兜底，并把轮播/灯箱手势监听放到 capture 阶段，兼容微信等内置浏览器中子元素抢事件的情况；touchmove 不再主动 `preventDefault`，避免 passive listener 警告。
- 验证：`pnpm test`、`pnpm build` 均通过；390px 手机视口下，真实 touch 左滑首页轮播从「活动」切到「蘑菇旅行」；`IP 鼠小仓` PDF 首图渲染比例 `1.412`，与图片自然比例和清单比例一致；PDF 灯箱从 `1 / 11` 左滑到 `2 / 11`，无 console error/page error。

### R2 视频流量迁移

- 根据用户授权，为 Cloudflare R2 桶 `jhinh-zuopinji` 启用公共开发 URL：`https://pub-f64a84b8652848a2a66fa67fc61cb99f.r2.dev`，用于浏览器直接读取作品集视频。
- 仅将 `public/data/portfolio-items.json` 中「个人网站搭建 / 影视后期」目录的 13 条 `.mp4/.MP4` 地址从腾讯 COS 替换为对应 R2 地址。
- 图片、PDF、视频封面和所有页面样式保持原样；视频封面仍由本地 `/assets/video-posters/` 提供。
- 注意：R2 公共开发 URL 有速率限制，当前作为低成本过渡方案；如未来访问量明显增长，应改为将 R2 绑定到自定义子域名并配置缓存策略。

### 详情抽屉顶部响应式一致性修复

- 修复 iPad 与桌面端作品详情抽屉顶部栏仍以独立半透明方框悬于抽屉内侧的问题：顶部栏改为延展至抽屉边缘，移除多余的背景、毛玻璃和分隔线，使标题与抽屉主体成为连续界面。
- 保留手机端的既有全宽贴顶标题栏，避免桌面修复影响移动端阅读与操作。
- 本地预览已分别检查 1440px 桌面、1024px iPad 和 390px 手机宽度；三种宽度下详情抽屉均可正常显示，手机端仍保持贴顶结构。

### R2 自定义域名与缓存优化

- 将 R2 桶 `jhinh-zuopinji` 绑定到 `media.jhinh.top`，保持旧的 `r2.dev` 公共开发地址可用，作为生产验证完成前的回退入口。
- 仅替换 `public/data/portfolio-items.json` 中 13 条影视后期视频地址：从 `r2.dev` 改为 `media.jhinh.top`；图片、PDF、视频 poster、封面与页面样式均未改动。
- 在 `jhinh.top` 区域创建并部署缓存规则「R2 视频缓存 30 天」：匹配 `media.jhinh.top` 的 `.mp4` 请求，符合缓存条件并忽略源站缓存标头，边缘 TTL 为 1 个月。
- 验证自定义域名视频响应为 `HTTP 200`、`content-type: video/mp4` 且保留 `Accept-Ranges: bytes`。HEAD 探测响应显示 `cf-cache-status: DYNAMIC`；视频播放器的 Range 请求与完整 GET 缓存行为可能不同，后续以 Cloudflare 控制台真实流量指标为准。
- 检查 Tiered Cache 页面后未启用 Smart Tiered Cache：控制台当前明确提示其归属于 Smart Shield，为避免偏离“不启用任何付费 Cloudflare 产品”的约束，保留默认状态。

### Safari 视频兼容性审计与修复

- 对 R2 自定义域名 `media.jhinh.top` 下 13 条影视后期视频完成 Safari 播放链路审计，检查视频编码、音频编码、`moov` 索引位置和 Range 响应。
- 其中 11 条文件已为前置索引；`江西广新 企业拜年.mp4` 与 `江西广新 企业宣传片.mp4` 的 `moov` 索引位于文件尾部，存在 iOS Safari 首播等待或加载失败风险。
- 两条问题视频均保持原始 H.264 + AAC-LC 编码，使用无损重封装方式前置索引；不重编码画面或音频，不改动文件 URL、网页封面或页面样式。
- 通过 R2 同路径覆盖上传两条修复文件，并仅清除了这两条 `media.jhinh.top` 视频 URL 的 Cloudflare 边缘缓存；线上复核返回 `HTTP 206`、正确的 `Content-Range`、`video/mp4`，文件开头均为 `ftyp -> moov`，且缓存规则继续命中。
- 增加视频播放错误兜底：仅在浏览器实际触发视频错误时显示「在新标签打开视频」链接；正常播放界面不增加额外视觉元素。
- 验证 `jhinh.top` 已支持 HTTP/3（`alt-svc: h3`）并可完成 TLS 1.3 握手，无需在免费套餐中额外开启付费网络功能。
- R2 上传用的临时用户 API 令牌已在上传后立即删除。无损重封装的本地副本保留在 `tmp/videos/safari-faststart/`，受 `.gitignore` 排除，未提交到仓库。
