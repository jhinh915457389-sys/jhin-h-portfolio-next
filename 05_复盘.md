# 复盘

## 结果

已完成本地可预览的 Next 版个人作品集。首页、作品集页、JSON 数据、分类筛选、详情抽屉、图片弹窗、PDF 阅读、视频播放、联系方式和响应式布局均已实现。测试、构建和视觉检查均通过，并已生成最终源码交付包与部署交接说明。

## 做得好的地方

1. 新项目独立于旧项目，避免影响 `project001_个人作品集网页/`。
2. 数据清洗、PDF 转图片、视频 poster 与页面渲染分层清晰，后续替换数据成本较低。
3. `impeccable` 审计及时发现构建路径别名和抽屉遮罩问题，避免带着明显缺陷继续堆功能。
4. 最终封装时排除了依赖、构建产物、临时目录、历史备份和预览文件，交付包保留完整可运行源码但不携带无关体积。

## 踩坑与问题

1. 初次构建缺少 `jsconfig.json`，导致 `@/` 路径别名无法解析。
2. 抽屉遮罩使用 blur 后，在自动截图和移动端观感中压低了详情内容清晰度。
3. 视觉检查如果在抽屉动画刚开始时截图，容易误判为内容模糊。

## 根因

1. Next.js 裸项目不会自动配置 `@/` alias，需要显式补充。
2. `backdrop-filter` 在模态层叠场景中容易造成不可控的混合效果。
3. 动效界面做自动化截图时，必须等待动画稳定。

## 下次复用

1. 新建 Next 项目时同步添加 `jsconfig.json` 或避免使用未配置别名。
2. 抽屉/弹窗优先保证内容清晰，背景氛围不要压过作品本身。
3. 视觉检查脚本需要覆盖动画等待、桌面端、移动端和关键交互路径。

## 需要同步到全局日志的内容

1. `backdrop-filter` 不适合作为抽屉遮罩的默认方案，容易让上层内容显得发糊。
2. 自动截图必须等待抽屉/弹窗动画完成，否则会记录到中间态。

## 后续维护建议

1. 上线前评估 COS 图片体积、CDN 缓存和移动端首屏加载速度。
2. 部署前再跑一次 `pnpm visual:check`，并用真实手机访问确认体验。
3. 若后续频繁更新作品，可继续把数据生成脚本封装成更明确的一键更新流程。
4. GitHub 建议作为 private repo 备份和版本回滚；国内稳定访问建议走腾讯云 COS + CDN，并提前处理域名、备案和 HTTPS。
